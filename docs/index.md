dd-virus-scan-cli
=================

Command-line client for the dd-virus-scan service

SYNOPSIS
--------

```bash
virus-scan start  { <PID> | <fileId> }
virus-scan status { <PID> | <fileId  }
```

For more information on a subcommand use:

```bash
virus-scan <subcommand> --help
```
