virusScanService:
  url: http://localhost:20325/
  httpClient:
    timeout: 30s
    connectionTimeout: 15s
    timeToLive: 1h
    retries: 2
#
# See https://www.dropwizard.io/en/latest/manual/configuration.html#logging
#
logging:
  level: INFO
  appenders:
    - type: file
      archive: false
      timeZone: system
      currentLogFilename: ${HOME}/dd-virus-scan-cli.log
